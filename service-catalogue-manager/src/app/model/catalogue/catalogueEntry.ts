import { HasCompetentAuthority } from './../services/hasCompetentAuthority';
export interface CatalogueEntry {
  catalogueId:string;
  name: string;
  country
  services
  status
  active
  description
}
