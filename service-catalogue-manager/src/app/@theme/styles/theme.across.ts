import { NbJSThemeOptions } from '@nebular/theme';

// const baseThemeVariables = baseTheme.variables;

export const ACROSS_THEME = {
  name: 'across',
  base: 'default',
  variables: {},
} as NbJSThemeOptions;
